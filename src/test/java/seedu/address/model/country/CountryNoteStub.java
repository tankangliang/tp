package seedu.address.model.country;

import seedu.address.model.note.CountryNote;

/**
 * A stub of Note which is an empty class
 */
public class CountryNoteStub extends CountryNote {

    public CountryNoteStub(String content, Country country) {
        super(content, country);
    }


}
